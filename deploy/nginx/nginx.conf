server {
    listen 80;
    server_name localhost;

    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
    }

    # Пример проксирования (если будет бэкенд)
    # location /api/ {
    #     proxy_pass http://backend:8000;
    # }
}
